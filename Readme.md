# Rainfall Prediction Project


## Overview
A production-style ML project for predicting whether it will rain the next day. Includes training, evaluation, CLI prediction, and a FastAPI server.


## Quickstart (Arch Linux)


1. Install system packages (recommended):


```bash
sudo pacman -S python python-pip python-numpy python-scipy python-pandas python-scikit-learn python-joblib
sudo pacman -S base-devel